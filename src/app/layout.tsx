import type { Metadata } from "next";
import { Linden_Hill, Quattrocento_Sans, Andada_Pro } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import { cn } from "@/lib/utils";

const LindenHill = Linden_Hill({
   variable: "--font-Linden_Hill",
   weight: ["400"],
   subsets: ["latin"],
});

const QuattrocentoSans = Quattrocento_Sans({
   variable: "--font-Quattrocento_Sans",
   weight: ["400", "700"],
   subsets: ["latin"],
});

const Andada = Andada_Pro({
   variable: "--font-Andada",
   weight: ["400", "500", "600", "700", "800"],
   subsets: ["latin"],
});
export const metadata: Metadata = {
   title: "Alooh Electrical",
   description: "Generated by create next app",
};

export default function RootLayout({
   children,
}: Readonly<{
   children: React.ReactNode;
}>) {
   return (
      <html
         lang="en"
         className={cn(
            "h-full",
            "antialiased",
            LindenHill.variable,
            QuattrocentoSans.variable,
            Andada.variable,
            "font-Quattrocento_Sans",
         )}
      >
         <body className="min-h-full flex flex-col" suppressHydrationWarning={true}>
            <Navbar />
            <main className="pt-20 flex-1">{children}</main>
            <Footer />
         </body>
      </html>
   );
}
